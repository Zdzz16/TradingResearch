# Direct dependencies only — pip pulls in everything else they need.
# Versions pinned to what this project was built and verified against.
pandas==2.3.3
yfinance==0.2.66
matplotlib==3.10.7
pytest==9.1.1
flask==3.1.3
